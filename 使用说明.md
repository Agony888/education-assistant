# Education Research Assistant v2.7 使用说明

## 一、先判断任务类型

### 单项任务

只需要一个结果时，直接调用模式：

```text
paper / review / method / theory / quality
gap / citation / proposal / thesis / obsidian
```

例如：

```text
请使用 education-research-assistant Skill，method 模式，design 深度，为题目《……》设计问卷维度。只完成问卷设计。
```

### 完整任务

先选择目标工作流：

- `paper-to-note`；
- `literature-review`；
- `proposal-design`；
- `thesis-design`。

再选择：

```text
A. guided：逐步引导后执行完整工作流
B. direct：根据现有材料立即执行所选工作流全部阶段
```

## 二、引导式怎么用

示例：

```text
请使用 education-research-assistant Skill，workflow 模式，执行 thesis-design，选择 guided。

我的方向是：乡镇小学数学教师数字化教学资源选择与整合能力。
请每次只问一个关键问题。等题目、研究对象、研究问题、数据、理论和方法梳理清楚后，再执行完整工作流。
```

引导式不会机械问完固定清单，而是根据回答动态选择下一问。

优先确认：

```text
研究对象与边界
→ 核心问题
→ 数据来源
→ 研究类型
→ 理论或分析维度
→ 方法与工具
→ 最终成果档位
```

## 三、直接式怎么用

```text
请使用 education-research-assistant Skill，workflow 模式，执行 thesis-design，选择 direct。

题目：《乡镇小学数学教师数字化教学资源选择与整合能力现状及提升策略研究》
研究对象：H市D县乡镇小学数学教师
可接触对象：约60名教师
可用方法：问卷、访谈

请直接执行 thesis-design 的全部阶段，不再提问。缺失信息、暂定判断和必要假设单独列出。
```

直接式会先整理：

- 已确认输入；
- 暂定输入；
- 缺失信息；
- 冲突；
- 必要假设。

然后执行所选工作流全部阶段。

## 四、引导式控制指令

| 指令 | 作用 |
|---|---|
| `查看进度` | 查看完成度和当前信息状态 |
| `修改：字段=内容` | 修改关键决定并检查下游影响 |
| `跳过` | 暂时跳过当前问题 |
| `不确定` | 标记为暂定 |
| `保存进度` | 输出可复制检查点 |
| `恢复进度` | 读取检查点继续 |
| `直接生成` | 切换到直接式执行同一工作流 |
| `重新开始` | 清空当前状态 |
| `结束引导` | 只总结当前信息 |

## 五、保存和恢复

保存：

```text
保存进度
```

Skill 会输出 YAML 检查点。

恢复：

```text
恢复进度
```

同时粘贴之前保存的检查点。恢复后不会重复询问已经确认的内容。

## 六、研究决策日志

以下决定会被记录：

- 研究中心；
- 研究对象与范围；
- 核心概念；
- 研究问题；
- 研究类型；
- 数据来源；
- 理论或分析框架；
- 方法和工具；
- 目录结构；
- 明确排除的方案。

例如输入：

```text
修改：研究对象=乡镇小学数学教师
```

Skill 不会只改这一处，还会检查题目、样本、问卷对象、研究问题和目录是否需要同步修改。

## 七、最终成果档位

### `outline`

只输出框架、研究对齐表和待确认事项。

### `standard`

默认档位，输出完整研究方案，适合与导师讨论。

### `submission-ready`

材料充分时，生成可直接修改提交的正式稿。材料不足时会降级。

### `review-ready`

输出正式成果，同时增加评委视角问题、风险和修改动作。

调用示例：

```text
请执行 proposal-design，选择 guided，最终成果档位为 review-ready。
```

## 八、四条工作流

### `paper-to-note`

```text
材料检查
→ 论文解析
→ 可选质量筛选
→ Obsidian 文献卡
→ 字段检查
```

### `literature-review`

```text
清点去重
→ 逐篇事实卡
→ 筛选
→ 矩阵
→ 聚类和研究脉络
→ 研究空间
→ 综述小节
→ 证据覆盖检查
```

### `proposal-design`

```text
题目和研究条件
→ 文献与研究空间
→ 研究类型
→ 理论框架
→ 方法和工具
→ 研究对齐表
→ 开题报告
→ 质量检查
```

### `thesis-design`

```text
题目优化
→ 概念边界
→ 研究问题
→ 研究类型和目录
→ 理论框架
→ 方法设计
→ 研究对齐表
→ 章节计划
→ 论文检查
```

## 九、常用单项命令

### 论文解析

```text
请使用 education-research-assistant Skill，paper 模式，standard 深度，解析这篇论文。区分作者明示、文本归纳和材料不足，关键判断标注页码、章节或表格位置。只完成论文解析。
```

### 文献矩阵

```text
请使用 education-research-assistant Skill，review 模式，matrix 深度，清点和去重这些文献，建立逐篇事实账本和比较矩阵。无法确认的信息不要补齐。
```

### 问卷维度

```text
请使用 education-research-assistant Skill，method 模式，design 深度，为题目《……》设计问卷维度。先说明核心概念和维度来源，再输出操作性定义、题项方向、依据和对应研究问题。
```

### 理论框架

```text
请使用 education-research-assistant Skill，theory 模式，framework 深度，为题目《……》设计理论或分析框架。说明理论如何进入概念、维度、工具、数据解释和论文结构。
```

## 十、材料不足时

以下情况不会强行生成确定结论：

- 只有摘要，无法判断方法细节；
- 作者、年份或来源无法确认；
- 研究问题没有可获得的数据；
- 题目要求效果研究但没有实验条件；
- 问卷维度没有文献或理论依据；
- 用户要求 `submission-ready`，但材料只支持 `standard`。

## 十一、完整提示词

见：

```text
docs/prompt-manual.md
```
